<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Post Detail</title>
  </head>
  <body>
    <div>
      <p><%= post.post_id%></p>
      <p><%= post.title%></p>
      <% if (post.images) { %>
      <div class="post-images">
        <img src="/files/<%= post.images[0].split('\\').pop() %>" />
      </div>
      <% } %>
    </div>
  </body>
</html>
